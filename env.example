# Application Configuration
APP_NAME=Muckard FastAPI Backend
DEBUG=True
API_V1_PREFIX=/api/v1

# Security
# SECRET_KEY will be auto-generated if not set
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/muckard_db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
CACHE_TTL=3600

# HashiCorp Vault (Optional for development)
VAULT_URL=http://localhost:8200
VAULT_TOKEN=
VAULT_MOUNT_PATH=secret

# Kraken API
KRAKEN_API_BASE_URL=https://api.kraken.com

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://dev.muckard.com,https://admin.muckard.com

# Email (Resend API)
# Get your API key from https://resend.com/api-keys
# Required for OTP email verification
APP_NAME=Muckard FastAPI Backend
DEBUG=True
API_V1_PREFIX=/api/v1

# Security
SECRET_KEY=  # Leave empty to auto-generate
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database (REQUIRED)
DATABASE_URL=postgresql://postgres:postgress@localhost:5433/muckard_db

# Redis (REQUIRED)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Vault (Optional for development)
VAULT_URL=http://localhost:8200
VAULT_TOKEN=
VAULT_MOUNT_PATH=secret

# Kraken API
KRAKEN_API_BASE_URL=https://api.kraken.com

# CORS (REQUIRED)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://dev.muckard.com,https://admin.muckard.com
RESEND_KEY=re_hL8A6acK_3KmFaPThw31LFbXaPkbyScCc


# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_SECURITY_PROTOCOL=PLAINTEXT
KAFKA_SASL_MECHANISM=
KAFKA_SASL_USERNAME=
KAFKA_SASL_PASSWORD=
KAFKA_ONBOARDING_TOPIC=onboarding.completed
KAFKA_ENABLED=True

